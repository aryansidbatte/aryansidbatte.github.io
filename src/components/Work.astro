---
import { projects } from '../content/projects'
import WorkInteractive from './WorkInteractive.tsx'
import ScrollReveal from './ScrollReveal.astro'
---

<section id="work" class="py-24 px-6 border-t border-border">
  <div class="max-w-4xl mx-auto">
    <ScrollReveal>
      <p class="font-mono text-[10px] tracking-[2px] text-muted uppercase mb-10">Work</p>
    </ScrollReveal>
    <WorkInteractive projects={projects} client:idle />
  </div>
</section>
